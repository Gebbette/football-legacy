(function(){
'use strict';
const $=s=>document.querySelector(s),screens=[...document.querySelectorAll('.mpScreen')],MAX_PLAYERS=6,MAX_SEASONS=10,ROOM_PREFIX='football-legacy-room-';
let peer=null,hostConnection=null,connections=new Map(),isHost=false,myPeerId='',myName='',localState=null,choiceSent=false,selectedChoice=null,statusTimer=null,revealTimer=null;
const POS_LABEL={ST:'Striker',WG:'Winger',CM:'Central Midfielder',CB:'Centre Back',GK:'Goalkeeper'};
const PROFILES=[
 {name:'Patient Prospect',rarity:'common',chance:17,w:170,ovr:[58,61],pot:[76,82],growth:.94},{name:'First-Team Project',rarity:'common',chance:16,w:160,ovr:[59,62],pot:[78,84],growth:.98},{name:'Steady Foundation',rarity:'common',chance:15,w:150,ovr:[60,63],pot:[80,85],growth:1},{name:'Physical Riser',rarity:'common',chance:10,w:100,ovr:[59,63],pot:[79,86],growth:1.02},{name:'Hidden Upside',rarity:'uncommon',chance:9,w:90,ovr:[59,62],pot:[82,88],growth:1.04},{name:'Late Spark',rarity:'uncommon',chance:7.5,w:75,ovr:[57,61],pot:[81,89],growth:.97},{name:'Big-Match Talent',rarity:'uncommon',chance:6,w:60,ovr:[61,64],pot:[82,89],growth:1.04},{name:'Reinvention Path',rarity:'uncommon',chance:4.5,w:45,ovr:[59,64],pot:[80,91],growth:1.01},{name:'Accelerated Rise',rarity:'rare',chance:4.5,w:45,ovr:[63,66],pot:[86,92],growth:1.10},{name:'High-Ceiling Wildcard',rarity:'rare',chance:3.5,w:35,ovr:[61,66],pot:[85,94],growth:1.08},{name:'Elite Tools',rarity:'rare',chance:3,w:30,ovr:[64,67],pot:[89,94],growth:1.12},{name:'Relentless Prodigy',rarity:'epic',chance:2.2,w:22,ovr:[65,68],pot:[91,96],growth:1.17,historic:.004},{name:'Golden Generation Talent',rarity:'epic',chance:1,w:10,ovr:[66,69],pot:[93,97],growth:1.20,historic:.008},{name:'Generational Outlier',rarity:'legendary',chance:.7,w:7,ovr:[67,70],pot:[95,99],growth:1.25,historic:.018},{name:'Unwritten Ceiling',rarity:'mythic',chance:.1,w:1,ovr:[68,71],pot:[96,99],growth:1.29,historic:.045}
];
const SCENARIOS=[
 {icon:'🏋️',title:'The extra session',text:'The coaches offer one final training block before the season.',options:[{label:'Push to the limit',hint:'High upside. Less control.',base:.22,swing:.78,dev:.42},{label:'Train with purpose',hint:'Balanced progress.',base:.28,swing:.42,dev:.25},{label:'Protect your fitness',hint:'Safer season. Smaller leap.',base:.18,swing:.23,apps:2}]},
 {icon:'🎙️',title:'The pressure question',text:'A reporter asks whether you can become the face of the club.',options:[{label:'Promise a huge season',hint:'Bold. The pressure rises.',base:.18,swing:.72,rep:4},{label:'Let football answer',hint:'Calm and balanced.',base:.26,swing:.38,rep:2},{label:'Praise the whole team',hint:'Safe. Less spotlight.',base:.17,swing:.24,trophy:.03}]},
 {icon:'👕',title:'The shirt is available',text:'A bigger squad role is there, but only if you demand it now.',options:[{label:'Demand to start',hint:'More games if it works.',base:.18,swing:.67,apps:5},{label:'Earn it quietly',hint:'Steady trust.',base:.27,swing:.36,apps:2},{label:'Focus on development',hint:'Fewer games. More growth.',base:.20,swing:.31,apps:-2,dev:.48}]},
 {icon:'✈️',title:'A major transfer call',text:'A stronger club makes an offer. Your role is not guaranteed.',transferClub:'Borussia Dortmund',options:[{label:'Accept the transfer',hint:'Bigger stage. Fiercer competition.',base:.18,swing:.75,transfer:true,rep:5},{label:'Ask for a bigger role',hint:'The deal could improve or collapse.',base:.16,swing:.88,transfer:true,apps:3},{label:'Stay and lead',hint:'More security at your club.',base:.27,swing:.34,apps:2,trophy:.03}]},
 {icon:'🧊',title:'The body feels heavy',text:'The schedule is building and one choice must be made.',options:[{label:'Play through it',hint:'More matches. More risk.',base:.10,swing:.73,apps:4},{label:'Use a managed plan',hint:'Balanced recovery.',base:.27,swing:.34,apps:1},{label:'Take a full reset',hint:'Fewer games. Better form odds.',base:.22,swing:.42,apps:-3,dev:.18}]},
 {icon:'©️',title:'The captain looks at you',text:'The dressing room needs someone to speak before a decisive run.',options:[{label:'Take command',hint:'A defining leadership moment.',base:.23,swing:.62,rep:5,trophy:.04},{label:'Lead through actions',hint:'Reliable influence.',base:.29,swing:.35,rep:2},{label:'Stay focused on yourself',hint:'Personal upside. Team risk.',base:.13,swing:.61,dev:.31}]},
 {icon:'🎯',title:'The tactical gamble',text:'The manager offers you freedom in the biggest matches.',options:[{label:'Attack every opening',hint:'Explosive but volatile.',base:.20,swing:.80,attack:.42},{label:'Choose your moments',hint:'Controlled influence.',base:.29,swing:.40,attack:.20},{label:'Keep the team shape',hint:'Safer team outcome.',base:.21,swing:.25,trophy:.05}]},
 {icon:'📄',title:'The contract deadline',text:'Your club wants an answer before the season begins.',options:[{label:'Sign immediately',hint:'Security and trust.',base:.23,swing:.31,apps:2},{label:'Negotiate hard',hint:'Greater reward or lost focus.',base:.17,swing:.73,rep:4},{label:'Run the deal down',hint:'A risky route to a bigger move.',base:.13,swing:.84,transfer:true,rep:3}]},
 {icon:'🌍',title:'The international summer',text:'Club preparation clashes with a chance to impress your country.',options:[{label:'Chase the tournament',hint:'Global stage. Heavy workload.',base:.20,swing:.68,award:.05},{label:'Balance both camps',hint:'The measured route.',base:.28,swing:.37},{label:'Prioritise the club',hint:'Better domestic preparation.',base:.22,swing:.27,apps:2,trophy:.03}]},
 {icon:'🔥',title:'The final season begins',text:'Ten years have led to one last chance to define the career.',options:[{label:'Risk everything',hint:'The highest ceiling and lowest floor.',base:.22,swing:.92,attack:.48,award:.06},{label:'Trust the complete game',hint:'A balanced final push.',base:.32,swing:.42,trophy:.04},{label:'Protect the legacy',hint:'Secure what you have.',base:.20,swing:.20,apps:2}]}
];
function hash(v){let h=2166136261>>>0;for(const c of String(v)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0}
function rng(key){return hash(key)/4294967296}
function rint(key,a,b){return a+Math.floor(rng(key)*(b-a+1))}
function clamp(n,a,b){return Math.max(a,Math.min(b,n))}
function cleanName(v){return String(v||'').replace(/[^\p{L}\p{N} ._'-]/gu,'').trim().slice(0,18)}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function initials(v){return String(v||'?').split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()}
function code(){const chars='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';return Array.from({length:5},()=>chars[Math.floor(Math.random()*chars.length)]).join('')}
function setScreen(id){screens.forEach(x=>x.classList.toggle('active',x.id===id))}
function notice(text,error=false){const el=$('#connectionStatus');el.textContent=text;el.className=`mpStatus show${error?' error':''}`;clearTimeout(statusTimer);statusTimer=setTimeout(()=>el.className='mpStatus',2600)}
function weightedProfile(roomCode){let q=(hash(roomCode+'|profile')%1000)+1;for(const p of PROFILES){q-=p.w;if(q<=0)return p}return PROFILES[0]}
function publicRoom(){if(!localState)return null;return {...localState,players:localState.players.map(p=>({...p,ready:p.choice!=null,choice:localState.phase==='reveal'||localState.phase==='final'?p.choice:null})),results:localState.results||[]}}
function send(conn,msg){try{if(conn?.open)conn.send(msg)}catch(e){}}
function broadcast(msg){for(const conn of connections.values())send(conn,msg)}
function publish(){if(!isHost)return;const payload={type:'state',room:publicRoom()};broadcast(payload);render()}
function makePlayer(id,name,profile){return {id,name,connected:true,choice:null,age:17,ovr:profile.startOvr,pot:profile.startPot,peak:profile.startOvr,club:'Hometown FC',apps:0,goals:0,assists:0,trophies:0,awards:0,legacy:0,last:null}}
function newRoom(roomCode,position,name){const profile=weightedProfile(roomCode),startOvr=rint(roomCode+'|ovr',profile.ovr[0],profile.ovr[1]),startPot=rint(roomCode+'|pot',Math.max(startOvr+7,profile.pot[0]),profile.pot[1]),shared={...profile,startOvr,startPot};return {code:roomCode,position,profile:shared,phase:'lobby',round:0,maxSeasons:MAX_SEASONS,players:[makePlayer(myPeerId,name,shared)],scenario:null,results:[],created:Date.now()}}
function peerReady(){return typeof window.Peer==='function'}
function wireConnection(conn,hostSide){connections.set(conn.peer,conn);conn.on('data',data=>hostSide?handleHostData(conn,data):handleClientData(data));conn.on('close',()=>{connections.delete(conn.peer);if(hostSide&&localState){const p=localState.players.find(x=>x.id===conn.peer);if(p){if(localState.phase==='lobby')localState.players=localState.players.filter(x=>x.id!==conn.peer);else p.connected=false;publish()}}else if(!hostSide)notice('The host disconnected. The room has ended.',true)});conn.on('error',()=>notice('A player connection was interrupted.',true))}
function createRoom(){myName=cleanName($('#hostName').value);if(!myName)return notice('Enter your nickname first.',true);if(!peerReady())return notice('Multiplayer is still loading. Try again.',true);isHost=true;const roomCode=code();peer=new Peer(ROOM_PREFIX+roomCode.toLowerCase(),{debug:1});peer.on('open',id=>{myPeerId=id;localState=newRoom(roomCode,$('#sharedPosition').value,myName);setScreen('lobby');render();notice('Room created. Share the code.');window.flTrack?.('Multiplayer Room Created')});peer.on('connection',conn=>wireConnection(conn,true));peer.on('error',err=>{if(err?.type==='unavailable-id'){peer.destroy();createRoom()}else notice('The room could not connect. Please try again.',true)})}
function joinRoom(){myName=cleanName($('#joinName').value);const roomCode=$('#roomCodeInput').value.toUpperCase().replace(/[^A-Z2-9]/g,'').slice(0,5);if(!myName)return notice('Enter your nickname first.',true);if(roomCode.length!==5)return notice('Enter the five-character room code.',true);if(!peerReady())return notice('Multiplayer is still loading. Try again.',true);isHost=false;peer=new Peer(undefined,{debug:1});peer.on('open',id=>{myPeerId=id;hostConnection=peer.connect(ROOM_PREFIX+roomCode.toLowerCase(),{reliable:true});wireConnection(hostConnection,false);hostConnection.on('open',()=>{send(hostConnection,{type:'join',name:myName});notice('Connected to room.');window.flTrack?.('Multiplayer Room Joined')});setTimeout(()=>{if(!hostConnection.open)notice('Room not found. Check the code and host connection.',true)},7000)});peer.on('error',()=>notice('The room could not connect. Check the code.',true))}
function handleHostData(conn,data){if(!data||typeof data.type!=='string')return;if(data.type==='join'){if(localState.phase!=='lobby')return send(conn,{type:'error',message:'This career has already started.'});if(localState.players.length>=MAX_PLAYERS)return send(conn,{type:'error',message:'This room is full.'});const name=cleanName(data.name)||'Player';if(!localState.players.some(p=>p.id===conn.peer))localState.players.push(makePlayer(conn.peer,name,localState.profile));publish()}else if(data.type==='choice'){if(localState.phase!=='choice')return;const p=localState.players.find(x=>x.id===conn.peer),index=Number(data.index);if(p&&Number.isInteger(index)&&index>=0&&index<localState.scenario.options.length&&p.choice==null){p.choice=index;publish();scheduleRevealIfReady()}}else if(data.type==='reaction'){relayReaction(cleanName(localState.players.find(x=>x.id===conn.peer)?.name)||'Player',String(data.emoji||'').slice(0,3))}}
function handleClientData(data){if(data?.type==='state'&&data.room){const previousRound=localState?.round;localState=data.room;setScreen(localState.phase==='lobby'?'lobby':'game');if(localState.phase!=='choice'||previousRound!==localState.round){choiceSent=false;selectedChoice=null}render()}else if(data?.type==='reaction')showReaction(data.name,data.emoji);else if(data?.type==='error')notice(data.message||'The room could not be joined.',true)}
function startGame(){if(!isHost||localState.players.length<2)return notice('At least two players are needed.',true);localState.round=1;localState.phase='choice';localState.scenario=scenarioFor(1);localState.players.forEach(p=>p.choice=null);localState.results=[];publish();window.flTrack?.('Multiplayer Career Started')}
function scenarioFor(round){return SCENARIOS[(hash(localState.code+'|scenario|'+round)+round-1)%SCENARIOS.length]}
function submitChoice(index){if(!localState||localState.phase!=='choice'||choiceSent)return;choiceSent=true;selectedChoice=index;if(isHost){const me=localState.players.find(p=>p.id===myPeerId);if(me&&me.choice==null)me.choice=index;publish();scheduleRevealIfReady()}else{send(hostConnection,{type:'choice',index});render()}}
function connectedPlayers(){return localState.players.filter(p=>p.connected!==false)}
function allReady(){return connectedPlayers().length>=2&&connectedPlayers().every(p=>p.choice!=null)}
function scheduleRevealIfReady(){if(!isHost||!allReady()||revealTimer)return;revealTimer=setTimeout(()=>{revealTimer=null;revealSeason()},900)}
function grade(avg){return avg>=8?'A+':avg>=7.65?'A':avg>=7.3?'B+':avg>=6.95?'B':avg>=6.65?'C+':avg>=6.35?'C':'D'}
function verdict(score){return score>=.62?'EXCELLENT CHOICE':score>=.27?'GOOD CHOICE':score>=-.04?'MIXED CHOICE':'POOR CHOICE'}
function legacy(p){const production=p.goals+p.assists,target=localState.position==='GK'?1:localState.position==='CB'?210:localState.position==='CM'?350:560,ability=clamp((p.peak-58)/38*28,0,28),games=clamp(p.apps/430*18,0,18),output=localState.position==='GK'?clamp(p.apps/430*22,0,22):clamp(production/target*22,0,22),team=clamp(p.trophies*3,0,15),individual=clamp(p.awards*3,0,12),raw=ability+games+output+team+individual,perfect=p.peak>=95&&p.apps>=390&&p.trophies>=4&&p.awards>=3&&(localState.position==='GK'||production>=target*.9)&&raw>=97;return perfect?100:clamp(Math.round(raw),0,99)}
function simulatePlayer(p){const sc=localState.scenario,index=p.choice==null?1:p.choice,opt=sc.options[index],key=`${localState.code}|${localState.round}|${p.id}|${index}`,luck=(rng(key+'|luck')*2-1)*opt.swing,decision=opt.base+luck,age=p.age,gap=Math.max(0,p.pot-p.ovr),natural=age<=20?1.7+gap*.075:age<=23?1.15+gap*.055:age<=26?.65+gap*.035:age<=29?.18:age<=31?-.25:-.75,growth=Math.round(natural*localState.profile.growth+decision*2.1+(opt.dev||0));let delta=clamp(growth,age<=23?-1:-2,age<=23?6:4),projected=p.ovr+delta;if(projected>p.pot){if(decision>=.24&&age<=29)p.pot=clamp(projected+(decision>=.75?2:decision>=.46?1:0),p.pot,99);else{delta=p.pot-p.ovr;projected=p.pot}}if(p.pot-p.ovr<=2&&decision>=.38&&age<=29)p.pot=clamp(Math.max(p.pot,projected+1),p.pot,99);p.ovr=clamp(projected,48,p.pot);p.peak=Math.max(p.peak,p.ovr);const baseApps=30+Math.round((p.ovr-62)*.22)+(opt.apps||0)+rint(key+'|apps',-4,5),apps=clamp(baseApps,12,48),rates={ST:[.48,.16],WG:[.29,.27],CM:[.14,.25],CB:[.055,.075],GK:[.002,.025]}[localState.position],quality=clamp(.72+(p.ovr-55)/55,.7,1.55),attackBoost=1+(opt.attack||0)+(decision*.11),historicProb=.00035+(localState.profile.historic||0)+({epic:.001,legendary:.004,mythic:.012}[localState.profile.rarity]||0),historic=p.ovr>=85&&age>=20&&rng(key+'|historic')<historicProb;let goals=Math.max(0,Math.round(apps*rates[0]*quality*attackBoost*(.76+rng(key+'|goals')*.48))),assists=Math.max(0,Math.round(apps*rates[1]*quality*attackBoost*(.76+rng(key+'|assists')*.48)));if(historic){if(['ST','WG'].includes(localState.position)){const total=48+rint(key+'|era',0,20),share=localState.position==='ST'?.66:.52;goals=Math.max(goals,Math.round(total*share));assists=Math.max(assists,total-goals)}else if(localState.position==='CM'){const total=29+rint(key+'|era',0,17);goals=Math.max(goals,Math.round(total*.34));assists=Math.max(assists,total-goals)}}const avg=clamp(6.62+(p.ovr-65)*.014+decision*.28+(goals+assists)/Math.max(20,apps)*.10+(historic?.82:0),5.8,9.15),trophy=rng(key+'|trophy')<clamp(.06+(p.ovr-70)*.007+(opt.trophy||0)+Math.max(0,decision)*.05,.02,.36),award=historic||rng(key+'|award')<clamp(.025+(p.ovr-78)*.009+(avg-7)*.10+(opt.award||0),.01,.34);if(trophy)p.trophies++;if(award)p.awards++;if(opt.transfer&&decision>=.10)p.club=sc.transferClub||['Benfica','Brighton & Hove Albion','Bayer Leverkusen'][localState.round%3];p.apps+=apps;p.goals+=goals;p.assists+=assists;p.age++;p.legacy=legacy(p);p.last={choice:index,choiceLabel:opt.label,verdict:verdict(decision),score:decision,apps,goals,assists,avg:+avg.toFixed(2),grade:grade(avg),delta,trophy,award,historic,club:p.club,effect:`${opt.label} ${decision>=.62?'transformed the season':decision>=.27?'gave you an edge':decision>=-.04?'produced mixed results':'did not pay off this time'}.`};return {...p.last,id:p.id,name:p.name,ovr:p.ovr,pot:p.pot,legacy:p.legacy}}
function revealSeason(){if(!isHost||localState.phase!=='choice')return;for(const p of localState.players)if(p.choice==null)p.choice=1;localState.results=localState.players.map(simulatePlayer).sort((a,b)=>b.legacy-a.legacy||b.ovr-a.ovr);localState.phase=localState.round>=MAX_SEASONS?'final':'reveal';publish();if(localState.phase==='final')window.flTrack?.('Multiplayer Career Completed')}
function nextSeason(){if(!isHost||localState.phase!=='reveal')return;localState.round++;localState.phase='choice';localState.scenario=scenarioFor(localState.round);localState.results=[];localState.players.forEach(p=>p.choice=null);choiceSent=false;publish()}
function relayReaction(name,emoji){if(!['🔥','😱','👏','😂'].includes(emoji))return;broadcast({type:'reaction',name,emoji});showReaction(name,emoji)}
function react(emoji){if(isHost)relayReaction(myName,emoji);else send(hostConnection,{type:'reaction',emoji})}
function showReaction(name,emoji){const layer=$('#reactionLayer'),el=document.createElement('div');el.className='mpReactionPop';el.style.setProperty('--drift',Math.random());el.innerHTML=`${esc(emoji)}<small>${esc(name)}</small>`;layer.appendChild(el);setTimeout(()=>el.remove(),1900)}
function copyCode(){navigator.clipboard?.writeText(localState.code).then(()=>notice('Room code copied.')).catch(()=>notice(`Room code: ${localState.code}`))}
function playerRows(showReady=false){const sorted=showReady?localState.players:[...localState.players].sort((a,b)=>b.legacy-a.legacy||b.ovr-a.ovr);return sorted.map((p,i)=>showReady?`<div class="mpPlayer"><div class="mpAvatar">${esc(initials(p.name))}</div><div><b>${esc(p.name)}${p.id===myPeerId?' • You':''}</b><small>${p.id===localState.players[0].id?'HOST • ':''}${p.connected===false?'DISCONNECTED':(p.choice!=null||p.ready)?'CHOICE LOCKED':'READY TO PLAY'}</small></div><i class="mpReady ${p.choice!=null||p.ready?'on':''}"></i></div>`:`<div class="mpLeaderRow ${p.id===myPeerId?'you':''}"><div class="mpRank">${i+1}</div><div><b>${esc(p.name)}${p.id===myPeerId?' • You':''}</b><small>${esc(p.club)}</small></div><div class="mpMetric"><strong>${p.legacy}</strong><span>LEGACY</span></div><div class="mpMetric"><strong>${p.ovr}</strong><span>OVR</span></div><div class="mpMetric mpHideMobile"><strong>${p.goals}</strong><span>GOALS</span></div><div class="mpMetric mpHideMobile"><strong>${p.trophies}</strong><span>TROPHIES</span></div></div>`).join('')}
function profileCard(){const p=localState.profile;return `<div class="mpCard mpProfile rarity-${p.rarity}"><div class="mpProfileTop"><div><div class="mpEyebrow">SHARED CAREER PROFILE</div><h2>${esc(p.name)}</h2><div class="mpRarity">${esc(p.rarity)} • ${p.chance}% profile</div></div><div class="mpProfileOvr">${p.startOvr}<small>START OVR</small></div></div><div class="mpNote">Everyone starts at ${p.startOvr} OVR, ${p.startPot} dynamic potential and the same club level.</div></div>`}
function renderLobby(){const b=$('#lobby');b.innerHTML=`<div class="mpLobbyHead"><div><div class="mpEyebrow">WAITING ROOM</div><h1>${localState.players.length} of ${MAX_PLAYERS} players</h1></div><button class="mpRoomCode" id="copyRoom"><small>TAP TO COPY</small><strong>${localState.code}</strong></button></div>${profileCard()}<div class="mpCard"><div class="mpEyebrow">PLAYERS</div><div class="mpPlayerGrid" style="margin-top:10px">${playerRows(true)}</div>${isHost?`<button id="startGame" class="mpPrimary mpLobbyAction" ${localState.players.length<2?'disabled':''}>Start 10-Season Career</button><div class="mpNote">Share the code. Start when everyone is here.</div>`:'<div class="mpWaiting">Waiting for the host to start…</div>'}</div>`;$('#copyRoom').onclick=copyCode;$('#startGame')?.addEventListener('click',startGame)}
function reactionBar(){return `<div class="mpReactionBar"><span>React</span>${['🔥','😱','👏','😂'].map(x=>`<button data-react="${x}" aria-label="React ${x}">${x}</button>`).join('')}</div>`}
function renderChoice(){const b=$('#game'),sc=localState.scenario,me=localState.players.find(p=>p.id===myPeerId),ready=me?.choice!=null||me?.ready||choiceSent,readyCount=connectedPlayers().filter(p=>p.choice!=null||p.ready).length;b.innerHTML=`<div class="mpGameTop"><div><div class="mpRound">SEASON ${localState.round} OF ${MAX_SEASONS}</div><h2>${2026+localState.round-1}/${String(2027+localState.round-1).slice(-2)}</h2></div><div class="mpRoomCode"><small>ROOM</small><strong>${localState.code}</strong></div></div><div class="mpCard mpScenario"><div class="mpEyebrow">${sc.icon} SHARED DECISION</div><h1>${esc(sc.title)}</h1><p>${esc(sc.text)}</p><div class="mpChoices">${sc.options.map((o,i)=>`<button class="mpChoice ${(me?.choice===i||selectedChoice===i)?'selected':''}" data-choice="${i}" ${ready?'disabled':''}><b>${esc(o.label)}</b><span>${esc(o.hint)}</span></button>`).join('')}</div>${ready?'<div class="mpWaiting">Choice locked. Nobody can see it yet.</div>':''}</div><div class="mpCard"><div class="mpLobbyHead"><div><div class="mpEyebrow">CHOICES LOCKED</div><h2>${readyCount} / ${connectedPlayers().length} ready</h2></div></div><div class="mpPlayerGrid">${playerRows(true)}</div>${isHost&&allReady()?'<button id="revealNow" class="mpPrimary">Reveal Season Now</button>':''}</div><div class="mpCard"><div class="mpEyebrow">LIVE TABLE</div><div class="mpLeaderboard" style="margin-top:9px">${playerRows(false)}</div></div>${reactionBar()}`;b.querySelectorAll('[data-choice]').forEach(x=>x.onclick=()=>submitChoice(+x.dataset.choice));$('#revealNow')?.addEventListener('click',revealSeason);wireReactions(b)}
function renderReveal(){const b=$('#game'),winner=localState.results[0],final=localState.phase==='final',mine=localState.results.find(x=>x.id===myPeerId)||winner,ordered=[...localState.players].sort((a,b)=>b.legacy-a.legacy||b.ovr-a.ovr);if(final){const podium=[ordered[1],ordered[0],ordered[2]].filter(Boolean);b.innerHTML=`<div class="mpCard mpReveal"><div class="mpEyebrow">FINAL WHISTLE • TEN SEASONS COMPLETE</div><h1>${esc(ordered[0].name)} wins the room</h1><div class="mpVerdict">${ordered[0].legacy}/100 LEGACY</div><div class="mpFinalPodium">${podium.map((p,i)=>`<div class="mpPodium ${p===ordered[0]?'first':''}"><strong>${p===ordered[0]?'1':p===ordered[1]?'2':'3'}</strong><b>${esc(p.name)}</b><small>${p.legacy}/100 • ${p.peak} peak</small></div>`).join('')}</div></div><div class="mpCard"><div class="mpEyebrow">FINAL TABLE</div><div class="mpLeaderboard" style="margin-top:9px">${playerRows(false)}</div>${isHost?'<button id="newRoom" class="mpPrimary">Create Another Room</button>':''}</div>${reactionBar()}`;$('#newRoom')?.addEventListener('click',()=>location.reload());wireReactions(b);return}b.innerHTML=`<div class="mpGameTop"><div><div class="mpRound">SEASON ${localState.round} REVEAL</div><h2>Everybody chose. Now the truth.</h2></div><div class="mpRoomCode"><small>LEADER</small><strong>${esc(initials(winner.name))}</strong></div></div><div class="mpCard mpReveal"><div class="mpEyebrow">YOUR SEASON</div><h1>${esc(mine.grade)} • ${esc(mine.verdict.replace(' CHOICE',''))}</h1><div class="mpVerdict">${esc(mine.verdict)}</div><p>${esc(mine.effect)}</p>${mine.historic?'<div class="mpHistoric">🌠 ONCE-IN-AN-ERA NUMBERS • A career-defining statistical outlier.</div>':''}</div><div class="mpResultGrid">${localState.results.map(r=>`<article class="mpResult ${r.id===myPeerId?'you':''}"><div class="mpResultHead"><h3>${esc(r.name)}</h3><b>${esc(r.grade)}</b></div><p><b>${esc(r.choiceLabel)}</b><br>${esc(r.effect)}</p><div class="mpStats"><div><small>APPS</small><b>${r.apps}</b></div><div><small>GOALS</small><b>${r.goals}</b></div><div><small>ASSISTS</small><b>${r.assists}</b></div><div><small>OVR</small><b>${r.delta>=0?'+':''}${r.delta}</b></div></div>${r.trophy?'<div class="mpHistoric">🏆 Trophy won</div>':''}${r.award?'<div class="mpHistoric">⭐ Individual award won</div>':''}</article>`).join('')}</div><div class="mpCard"><div class="mpEyebrow">LIVE TABLE</div><div class="mpLeaderboard" style="margin-top:9px">${playerRows(false)}</div>${isHost?`<button id="nextSeason" class="mpPrimary">Play Season ${localState.round+1}</button>`:'<div class="mpWaiting">Waiting for the host to begin the next season…</div>'}</div>${reactionBar()}`;$('#nextSeason')?.addEventListener('click',nextSeason);wireReactions(b)}
function wireReactions(root){root.querySelectorAll('[data-react]').forEach(x=>x.onclick=()=>react(x.dataset.react))}
function render(){if(!localState)return;if(localState.phase==='lobby'){setScreen('lobby');renderLobby()}else{setScreen('game');if(localState.phase==='choice')renderChoice();else renderReveal()}}
function qaRun(playerCount=4,roomCode='QA7MX'){
 const keep={peer,isHost,myPeerId,myName,localState,choiceSent,selectedChoice,revealTimer};
 const failures=[];let hiddenChoices=true,maxScore=0,minScore=100,historic=0,potentialRaises=0;
 try{
  isHost=true;myPeerId='qa-0';myName='Player 1';localState=newRoom(roomCode,'ST',myName);
  const count=clamp(Math.round(playerCount),2,MAX_PLAYERS);
  for(let i=1;i<count;i++)localState.players.push(makePlayer(`qa-${i}`,`Player ${i+1}`,localState.profile));
  localState.round=1;localState.phase='choice';localState.scenario=scenarioFor(1);
  for(let round=1;round<=MAX_SEASONS;round++){
   localState.round=round;localState.phase='choice';localState.scenario=scenarioFor(round);localState.results=[];
   localState.players.forEach((p,i)=>p.choice=(i+round)%localState.scenario.options.length);
   const concealed=publicRoom();
   if(concealed.players.some(p=>p.choice!==null)||concealed.players.some(p=>!p.ready))hiddenChoices=false;
   const before=localState.players.map(p=>p.pot);
   localState.results=localState.players.map(simulatePlayer).sort((a,b)=>b.legacy-a.legacy||b.ovr-a.ovr);
   localState.phase=round===MAX_SEASONS?'final':'reveal';
   const revealed=publicRoom();
   if(revealed.players.some(p=>p.choice===null))failures.push(`round ${round} choices did not reveal`);
   localState.players.forEach((p,i)=>{if(p.ovr>p.pot)failures.push(`round ${round} OVR exceeded potential`);if(p.pot>before[i])potentialRaises++;if(p.last?.historic)historic++;maxScore=Math.max(maxScore,p.legacy);minScore=Math.min(minScore,p.legacy)});
  }
  if(!hiddenChoices)failures.push('choice privacy failed');
  if(localState.phase!=='final'||localState.round!==MAX_SEASONS)failures.push('career did not reach the final table');
  if(localState.players.length!==count)failures.push('player count changed');
  return {ok:failures.length===0,failures,players:count,seasons:MAX_SEASONS,profile:`${localState.profile.name} (${localState.profile.rarity})`,hiddenChoices,maxScore,minScore,potentialRaises,historic,finalPhase:localState.phase};
 }finally{
  ({peer,isHost,myPeerId,myName,localState,choiceSent,selectedChoice,revealTimer}=keep);
 }
}
window.FL_MULTIPLAYER_QA={run:qaRun,profiles:PROFILES.map(({name,rarity,chance,w})=>({name,rarity,chance,w})),scenarioCount:SCENARIOS.length};
$('#createRoom').onclick=createRoom;$('#joinRoom').onclick=joinRoom;$('#roomCodeInput').addEventListener('input',e=>e.target.value=e.target.value.toUpperCase().replace(/[^A-Z2-9]/g,'').slice(0,5));$('#helpBtn').onclick=()=>$('#helpModal').hidden=false;$('#closeHelp').onclick=()=>$('#helpModal').hidden=true;$('#helpModal').addEventListener('click',e=>{if(e.target.id==='helpModal')e.currentTarget.hidden=true});
})();
