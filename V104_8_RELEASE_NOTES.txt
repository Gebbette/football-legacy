FOOTBALL LEGACY V104.8 — MULTIPLAYER SURPRISE + RELIABILITY PASS

Multiplayer reveal
- Ballon d'Or world rankings are now the final row of the shared points reveal.
- Player columns stay in party JOIN ORDER until the points sequence has finished, so table position cannot spoil the result.
- The Ballon d'Or ceremony can still interrupt the reveal cinematically, but the actual rankings remain sealed until the end.
- Shared Ballon d'Or envelope sequence increased to ~22 seconds; the envelope/card has a long readable hold.
- Trophy presentation order is forced to trophies first, then individual awards.

Room flow
- Play With Friends opens with two large, obvious choices: CREATE ROOM and JOIN ROOM.
- No separate nickname field. The footballer name is used throughout multiplayer.
- Random rarity is now MYSTERY RARITY: the selected rarity is hidden before the career begins and then resolved once for the whole party, so every player receives the same surprise rarity.

Reliability / reconnection
- Persistent player-slot tokens survive refreshes and browser/app backgrounding.
- Presence heartbeat and automatic reconnect on visibility/focus/online/pageshow.
- Host rooms persist locally and can restore after a host refresh.
- Disconnected players keep their exact career/score slot during the reconnect grace period.
- A large RECONNECT TO GAME panel is shown when a client loses the room.
- Host can remove a player only after the saved-slot grace period, allowing the remaining party to continue.
- Multiplayer protocol bumped to 12 to prevent old and new state machines mixing.

End of career
- The final podium no longer appears immediately after the last season update.
- Every player first enters a FINAL REVIEW phase and can browse the Legacy tab and career progress.
- Each player actively presses READY FOR FINAL LEADERBOARD; the podium stays sealed until everyone is ready.
- Podium now shows LEGACY X/100 directly under each podium position.

Reviews
- The multiplayer final screen now contains a review form.
- The UI clearly states that reviews are sent to playfootballlegacy@gmail.com.
- Submissions use FormSubmit's AJAX endpoint with a mailto fallback.
- First-time FormSubmit activation for the destination inbox may be required before automatic delivery begins.

Regression checks
- Multiplayer internal QA: PASS.
- Protocol/synchronisation QA: PASS.
- Join-order reveal test: PASS.
- Mystery rarity shared-resolution test: PASS.
- Reconnect token reclaim test: PASS.
- Final-ready barrier test: PASS.
- Shared Ballon d'Or duration check: 22,000 ms.
- Ballon d'Or ranking category verified as final reveal category.
